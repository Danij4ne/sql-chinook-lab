
/*
CHALLENGE 010 — Filter Invoices by Date

Goal:
Return invoices issued after a specific date.

Instructions:
1) Query the table: invoice
2) Select exactly these columns (in this order):
   - invoice_id
   - customer_id
   - invoice_date
   - total
3) Filter rows where:
   - invoice_date >= '2012-01-01'
4) Sort the result by:
   - invoice_date ASC

Expected:
A result set with 4 columns:
invoice_id, customer_id, invoice_date, total
Only invoices with invoice_date on or after 2012-01-01, ordered by invoice_date ascending.
*/
