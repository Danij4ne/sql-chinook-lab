
/*
CHALLENGE 015 — Select Tracks by Price Range

Goal:
Return tracks whose unit price falls within a specific range.

Instructions:
1) Query the table: track
2) Select exactly these columns (in this order):
   - track_id
   - name
   - unit_price
3) Filter rows where:
   - unit_price BETWEEN 0.99 AND 1.99
4) Sort the result by:
   - unit_price ASC
   - name ASC

Expected:
A result set with 3 columns:
track_id, name, unit_price
Only tracks priced between 0.99 and 1.99, ordered by unit_price ascending and name ascending.
*/
