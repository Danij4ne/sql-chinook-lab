
/*
CHALLENGE 007 — Basic WHERE Conditions

Goal:
Return tracks that belong to a specific media type.

Instructions:
1) Query the table: track
2) Select exactly these columns (in this order):
   - track_id
   - name
   - media_type_id
3) Filter rows where:
   - media_type_id = 1
4) Sort the result by:
   - track_id ASC

Expected:
A result set with 3 columns:
track_id, name, media_type_id
Only tracks with media_type_id equal to 1, ordered by track_id ascending.
*/
