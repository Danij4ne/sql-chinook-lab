
/*
CHALLENGE 006 — Find Expensive Tracks

Goal:
Return tracks with a unit price higher than 1.00.

Instructions:
1) Query the table: track
2) Select exactly these columns (in this order):
   - track_id
   - name
   - unit_price
3) Filter rows where:
   - unit_price > 1.00
4) Sort the result by:
   - unit_price DESC
   - name ASC

Expected:
A result set with 3 columns:
track_id, name, unit_price
Only tracks with unit_price greater than 1.00, ordered by unit_price descending and name ascending.
*/
